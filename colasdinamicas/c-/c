using System;
usinng System.Collections.Generic;

class Practice {
    static void Main()
    {
        Stack<int> pila = new Stack<int>();
        pila.Push(10);
        pila.Push(20);
        pila.Push(30);

        Console.Writeline("pila:");
        while (pila.count > 0)

        {
            Console.Writeline(pila.pop());
    }
}