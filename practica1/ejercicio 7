using System; //Suma de 5 numeros ingresados por el usuario
int suma = 0;

for (int i = 1; i <= 5; i++)
{
    Console.Write($"Ingresa el número {i}: ");
    suma += int.Parse(Console.ReadLine());
}

Console.WriteLine($"La suma total es: {suma}");